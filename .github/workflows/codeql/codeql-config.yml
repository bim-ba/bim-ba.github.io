name: "CodeQL config"

queries:
  - name: security and quality
  uses: security-and-quality
  - name: custom queries
  uses: ./.github/workflows/codeql/queries

paths-ignore:
  - '/docs/'
  - '/public/'
  - '/sketch/'
